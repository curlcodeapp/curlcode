# Supabase (safe to expose to the client — access is enforced via Row Level Security)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Never put the Supabase service role key here or in any NEXT_PUBLIC_ variable.
# It must only be used server-side (Supabase Edge Functions / CI secrets).
